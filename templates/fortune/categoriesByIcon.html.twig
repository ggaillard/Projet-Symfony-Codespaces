{% extends 'base.html.twig' %}

{% block body %}

<h1 class="text-center my-8 text-2xl text-slate-800 font-bold">Catégories par icône</h1>
<p class="text-center">Catégories avec l'icône "{{ iconKey }}"</p>

<div class="xl:container xl:mx-auto grid grid-cols-2 gap-6 px-10 mt-8 mb-10">
    {# Affichez les catégories ici #}
</div>

{% endblock %}
